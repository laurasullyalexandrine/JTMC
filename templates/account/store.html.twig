{% extends 'base.html.twig' %}

{% block title %}Mes commerces - J'Trouve mon commerçant{% endblock %}

{% block body %}
    <a href="{{ path('account_store_add')}}" class="btn btn-info btn-sm float-right">Ajouter un commerce</a>
    <h1>Mes commerces</h1>
    Bienvenue {{ app.user.firstname }} dans votre compte.</br>
    C'est dans cet espace que vous allez pouvoir gérer vos commerces.
    <a href="{{ path('account')}}">Retour</a>
    <hr>
    {% if app.user.stores|length == 0 %}
    Vous n'avez pas encore ajouté de commerce dans votre compte. Veuillez <a href="{{ path('account_store_add')}}">cliquer ici</a>
    {% else %}
    <div class="row">
    {% for stores in  app.user.stores %}
    <div class="col-md-4">
        <div class="card" style="width: 18rem;">
        <div class="card-body">
        <h5 class="card-title">{{ stores.name }}</h5>
        <p class="card-text">{{ stores.address }}</p>
        <a href="{{ path('account_store_edit', { 'id' : stores.id }) }}">Modifier</a> | <a href="{{ path('account_store_delete', { 'id' : stores.id }) }}">Supprimer</a> | <a href="{{ path('account_store_show', { 'id' : stores.id }) }}">Afficher</a>
    </div>
    </div>
    </div>
    {% endfor %}
    </div>
    {% endif %}
{% endblock %}