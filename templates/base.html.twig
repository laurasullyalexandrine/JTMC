<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <title>{% block title %}{% endblock %}J'trouve mon commerçant</title>
    <link rel="stylesheet" href="{{ asset('css/app.css') }}">
</head>
    <body>
        <div class="container">

            <nav class="navbar navbar-light bg-light">
                <div>
                    <h1><a href="{{ path('question_list') }}">J'trouve mon commerçant</a></h1>
                    <div><h3>Soutenez les commerçants près de chez vous!</h3></div>
                </div>
                
                <div id="right">
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <span class="mr-2">Bienvenue <b>{{ app.user.username }}</b></span>
                    <a href="{{ path('question_add') }}" class="btn btn-danger"></a>
                    {% if is_granted('ROLE_MODERATOR') %}
                    <a href="{{ path('admin_user') }}" class="btn btn-success">Utilisateurs</a>
                    {% endif %}
                    <a href="{{ path('user_profile') }}" class="btn btn-primary">Mon compte</a>
                    <a href="{{ path('logout') }}" class="btn btn-secondary">Déconnexion</a>
                    {% else %}
                    <span class="mr-2">Bienvenue invité</span>
                    <a href="{{ path('user_register') }}" class="btn btn-success">Inscription</a>
                    <a href="{{ path('login') }}" class="btn btn-primary">Connexion</a>
                    {% endif %}

                </div>
            </nav>

            <div class="row">
                <div class="col">
                    {% block body %}{% endblock %}
                </div>
            </div>

            <footer>

               {#mention légale#} 
               {#contact#}
               
            </footer>
            
        </div>
    </body>


</html>
