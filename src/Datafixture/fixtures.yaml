App\Entity\CommercialService:
    service_{1..5}:
        servicetype: <'['RELAI RAPIDE', 'RELAI']'> 
App\Entity\InformationPayment:
    payment_{1..5}: 
        paymentTypes: '<creditCardType()>'
App\Entity\Store:
    store_{1..5}:
        store_activity (unique): '<word()>'
        picture: '<image()>'
        name: '<company()>'
        siret (unique): '<isbn13()>'
        address: '<address()>'
        road_number: '<buildingNumber()>'
        postal_code: '<postcode()>'
        email (unique): '<email()>'
        phone (unique): '<phoneNumber()>'
        website (unique): '<url()>'
        open_days (unique): '<dayOfWeek()>'
        open_hours: '<time()>'
        description: '<paragraphs()>'
App\Entity\Role:
    role_{admin, moderator, user}:
        name (unique): '<current()>'
        role: '<current()>'
App\Entity\User:
    user_{1..4}:
        email (unique): '<email()>'
        password (unique): '<password()>'
